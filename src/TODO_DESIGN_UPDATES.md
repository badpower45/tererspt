# قائمة التحديثات المتبقية للتصميم

## الصفحات المحدثة ✅

1. ✅ **App.tsx** - الهيدر الرئيسي
2. ✅ **Sidebar.tsx** - الشريط الجانبي
3. ✅ **MobileNav.tsx** - القائمة السفلية
4. ✅ **Dashboard.tsx** - لوحة التحكم
5. ✅ **Products.tsx** - صفحة المنتجات
6. ✅ **Login.tsx** - صفحة تسجيل الدخول
7. ✅ **globals.css** - نظام الألوان

## الصفحات التي تحتاج تحديث 🔄

### أولوية عالية
1. **Inventory.tsx** - إدارة المخزون
2. **Sales.tsx** - إدارة المبيعات
3. **GeneralPOS.tsx** - نقطة البيع
4. **Partners.tsx** - الشركاء

### أولوية متوسطة
5. **Barter.tsx** - نظام التبادل
6. **Installations.tsx** - التركيبات
7. **Branches.tsx** - الفروع
8. **Settings.tsx** - الإعدادات

### أولوية منخفضة
9. **PartnerProducts.tsx** - منتجات الشراكات
10. **ProductForm.tsx** - نموذج إضافة منتج

## التغييرات المطلوبة لكل صفحة

### الألوان
- استبدال `bg-blue-600` بـ `bg-slate-900`
- استبدال `bg-blue-700` بـ `bg-slate-800`
- استبدال `hover:bg-blue-700` بـ `hover:bg-slate-800`
- إزالة جميع الـ gradients مثل `gradient-to-br from-blue-600 to-blue-700`
- استخدام `bg-slate-50` للخلفيات الفاتحة
- استخدام `text-slate-900` للنصوص الداكنة
- استخدام `text-slate-600` للنصوص الثانوية

### النصوص
- العناوين: `text-2xl text-slate-900`
- العناوين الفرعية: `text-sm text-slate-600`
- الأزرار: يجب أن تكون واضحة بدون gradients

### الأزرار
```tsx
// قبل
<button className="bg-blue-600 hover:bg-blue-700">

// بعد
<button className="bg-slate-900 hover:bg-slate-800">
```

### البطاقات
```tsx
// قبل
<div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6">

// بعد (نفس الشيء - جيد كما هو)
<div className="bg-white rounded-lg border border-gray-200 p-6 hover:border-gray-300 transition-all">
```

### الأيقونات في البطاقات
```tsx
// قبل
<div className="w-12 h-12 bg-blue-100 rounded-lg">
  <Icon className="w-6 h-6 text-blue-600" />
</div>

// بعد
<div className="w-12 h-12 bg-slate-900 rounded-lg">
  <Icon className="w-6 h-6 text-white" />
</div>
```

## ملاحظات مهمة

1. الاحتفاظ بالألوان الدلالية:
   - أخضر للنجاح: `bg-green-600`
   - أحمر للخطر: `bg-red-600`
   - كهرماني للتحذير: `bg-amber-600`
   - أزرق للمعلومات: `bg-blue-600`

2. الترجمة للعربية:
   - تغيير جميع النصوص الإنجليزية إلى العربية
   - التأكد من اتجاه RTL

3. التناسق:
   - استخدام نفس المسافات (spacing)
   - استخدام نفس أحجام الخطوط
   - استخدام نفس نمط الحدود

## كيفية التحديث

1. افتح الملف
2. ابحث عن `bg-blue-600` و `bg-blue-700`
3. استبدلها بـ `bg-slate-900` و `bg-slate-800`
4. ابحث عن `gradient`
5. احذف جميع الـ gradients واستخدم ألوان صلبة
6. تأكد من النصوص العربية
7. احفظ واختبر

## أمثلة سريعة

### مثال 1: زر رئيسي
```tsx
// قبل
<Button className="bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white">

// بعد
<Button className="bg-slate-900 hover:bg-slate-800 text-white">
```

### مثال 2: بطاقة إحصائيات
```tsx
// قبل
<div className="bg-gradient-to-br from-blue-50 to-indigo-50 p-4">

// بعد
<div className="bg-slate-50 p-4">
```

### مثال 3: عنوان
```tsx
// قبل
<h1 className="text-transparent bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text">

// بعد
<h1 className="text-2xl text-slate-900">
```
